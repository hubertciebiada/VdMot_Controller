// The real ArduinoJson (bblanchon/ArduinoJson, the version pinned in platformio.ini), vendored as a
// single header in test/native/third_party; glue/CMakeLists.txt stops when the versions differ.
// The ARDUINOJSON_* switches of platformio.ini come from the glue compile definitions.
#pragma once

#include <ArduinoJson-v6.21.6.h>
