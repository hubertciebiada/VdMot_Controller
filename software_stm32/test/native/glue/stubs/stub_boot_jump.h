// Link-seam stub of src/boot_jump.cpp (include/boot_jump.h), which is not part of the glue suite
// (it jumps into the ROM bootloader): JumpToBootloader() logs and throws fake::BootloaderJump.
#pragma once

#include "boot_jump.h"
#include "fake_board.h"
#include "stub_log.h"
