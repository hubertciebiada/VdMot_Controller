{
  "name": "vdm_core",
  "version": "2.0.0",
  "description": "Hardware-free core logic of the VdMot STM32 firmware (line assembler, tokenizer, argument parsing, reply formatting, 1-Wire checks, stall detection, end-stop detection, calibration, move classification, current profiles, motor parameters, EEPROM layout, system statistics)",
  "frameworks": "*",
  "platforms": "*",
  "build": {
    "srcDir": "src",
    "includeDir": "include"
  }
}
